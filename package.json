{
  "name": "ai-browser-bridge",
  "version": "0.1.0",
  "description": "Local bridge between an AI coding agent (or any local tool) and your logged-in Chrome: tabs, CSP-proof eval, trusted input, authenticated downloads, print-to-PDF. No cloud, localhost only.",
  "author": "Mykola Bielousov",
  "license": "MIT",
  "main": "server/server.js",
  "bin": {
    "ai-bridge-server": "server/server.js",
    "bridge": "server/cli.js"
  },
  "scripts": {
    "start": "node server/server.js",
    "test": "node test/protocol.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "keywords": ["chrome-extension", "browser-automation", "ai-agent", "devtools-protocol", "claude", "bridge"],
  "engines": { "node": ">=18" }
}
